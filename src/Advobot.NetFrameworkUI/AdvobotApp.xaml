<Application x:Class="Advobot.NetFrameworkUI.AdvobotApp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:classes="clr-namespace:Advobot.NetFrameworkUI.Classes"
             xmlns:controls="clr-namespace:Advobot.NetFrameworkUI.Classes.Controls"
             xmlns:converters="clr-namespace:Advobot.NetFrameworkUI.Classes.Converters"
             xmlns:validationrules="clr-namespace:Advobot.NetFrameworkUI.Classes.ValidationRules"
             xmlns:shared="clr-namespace:Advobot.SharedUI;assembly=Advobot.Core"
             mc:Ignorable="d"
             Startup="OnStartup">

    <Application.Resources>
        <!-- Converters -->
        <converters:UriConverter x:Key="UriConverter" />
        <converters:NetFrameworkNullToBoolConverter x:Key="NullToBoolConverter" />
        <converters:EnumerableConverter x:Key="EnumerableConverter" />
        <converters:NetFrameworkColorConverter x:Key="ColorConverter" />
        <!-- Validators -->
        <validationrules:ColorValidationRule x:Key="ColorValidationRule" />
        <!-- Colors -->
        <x:Static x:Key="{x:Static shared:ColorTargets.BaseBackground}"
                  Member="classes:NetFrameworkColorSettings.LightModeBaseBackground" />
        <x:Static x:Key="{x:Static shared:ColorTargets.BaseForeground}"
                  Member="classes:NetFrameworkColorSettings.LightModeBaseForeground" />
        <x:Static x:Key="{x:Static shared:ColorTargets.BaseBorder}"
                  Member="classes:NetFrameworkColorSettings.LightModeBaseBorder" />
        <x:Static x:Key="{x:Static shared:ColorTargets.ButtonBackground}"
                  Member="classes:NetFrameworkColorSettings.LightModeButtonBackground" />
        <x:Static x:Key="{x:Static shared:ColorTargets.ButtonForeground}"
                  Member="classes:NetFrameworkColorSettings..LightModeButtonForeground" />
        <x:Static x:Key="{x:Static shared:ColorTargets.ButtonBorder}"
                  Member="classes:NetFrameworkColorSettings.LightModeButtonBorder" />
        <x:Static x:Key="{x:Static shared:ColorTargets.ButtonDisabledBackground}"
                  Member="classes:NetFrameworkColorSettings.LightModeButtonDisabledBackground" />
        <x:Static x:Key="{x:Static shared:ColorTargets.ButtonDisabledForeground}"
                  Member="classes:NetFrameworkColorSettings.LightModeButtonDisabledForeground" />
        <x:Static x:Key="{x:Static shared:ColorTargets.ButtonDisabledBorder}"
                  Member="classes:NetFrameworkColorSettings.LightModeButtonDisabledBorder" />
        <x:Static x:Key="{x:Static shared:ColorTargets.ButtonMouseOverBackground}"
                  Member="classes:NetFrameworkColorSettings.LightModeButtonMouseOverBackground" />
        <!-- Templates -->
        <ControlTemplate x:Key="ButtonControlTemplate"
                         TargetType="ButtonBase">
            <Border Name="ButtonControlTemplateBorder"
                    BorderThickness="1"
                    Background="{DynamicResource {x:Static shared:ColorTargets.ButtonBackground}}"
                    BorderBrush="{DynamicResource {x:Static shared:ColorTargets.ButtonBorder}}">
                <ContentPresenter x:Name="ButtonControlTemplateBorderContentPresenter"
                                  Margin="2"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  RecognizesAccessKey="True"
                                  TextBlock.Foreground="{DynamicResource {x:Static shared:ColorTargets.ButtonForeground}}" />
            </Border>

            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter TargetName="ButtonControlTemplateBorder"
                            Property="Background"
                            Value="{DynamicResource {x:Static shared:ColorTargets.ButtonMouseOverBackground}}" />
                </Trigger>
                <Trigger Property="IsEnabled"
                         Value="False">
                    <Setter TargetName="ButtonControlTemplateBorder"
                            Property="Background"
                            Value="{DynamicResource {x:Static shared:ColorTargets.ButtonDisabledBackground}}" />
                    <Setter TargetName="ButtonControlTemplateBorder"
                            Property="BorderBrush"
                            Value="{DynamicResource {x:Static shared:ColorTargets.ButtonDisabledBorder}}" />
                    <Setter TargetName="ButtonControlTemplateBorderContentPresenter"
                            Property="TextBlock.Foreground"
                            Value="{DynamicResource {x:Static shared:ColorTargets.ButtonDisabledForeground}}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        <!-- Styles -->
        <Style TargetType="Control">
            <Setter Property="Background"
                    Value="{DynamicResource {x:Static shared:ColorTargets.BaseBackground}}" />
            <Setter Property="Foreground"
                    Value="{DynamicResource {x:Static shared:ColorTargets.BaseForeground}}" />
            <Setter Property="BorderBrush"
                    Value="{DynamicResource {x:Static shared:ColorTargets.BaseBorder}}" />
        </Style>
        <Style TargetType="controls:AdvobotTextBox"
               BasedOn="{StaticResource {x:Type Control}}" />
        <Style TargetType="controls:AdvobotTextEditor"
               BasedOn="{StaticResource {x:Type Control}}" />
        <Style TargetType="controls:AdvobotRichTextBox"
               BasedOn="{StaticResource {x:Type Control}}" />
        <Style TargetType="controls:AdvobotTreeView"
               BasedOn="{StaticResource {x:Type Control}}" />
        <Style TargetType="controls:AdvobotComboBox"
               BasedOn="{StaticResource {x:Type Control}}">
            <Setter Property="Foreground"
                    Value="Black" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Center" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem"
                           BasedOn="{StaticResource {x:Type Control}}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate x:Name="ComboBoxItemTemplate"
                                                 TargetType="ComboBoxItem">
                                    <Border Name="ComboBoxItemTemplateBorder"
                                            BorderThickness="1"
                                            Background="{DynamicResource {x:Static shared:ColorTargets.ButtonBackground}}"
                                            BorderBrush="{DynamicResource {x:Static shared:ColorTargets.ButtonBorder}}">
                                        <ContentPresenter x:Name="ComboBoxItemTemplateBorderContentPresenter"
                                                          Margin="2"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          RecognizesAccessKey="True"
                                                          TextBlock.Foreground="{DynamicResource {x:Static shared:ColorTargets.ButtonForeground}}" />
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver"
                                                 Value="True">
                                            <Setter TargetName="ComboBoxItemTemplateBorder"
                                                    Property="Background"
                                                    Value="{DynamicResource {x:Static shared:ColorTargets.ButtonMouseOverBackground}}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="controls:AdvobotGrid">
            <Setter Property="Control.Background"
                    Value="{DynamicResource {x:Static shared:ColorTargets.BaseBackground}}" />
            <Setter Property="Control.Foreground"
                    Value="{DynamicResource {x:Static shared:ColorTargets.BaseForeground}}" />
            <Setter Property="Control.BorderBrush"
                    Value="{DynamicResource {x:Static shared:ColorTargets.BaseBorder}}" />
        </Style>
        <Style TargetType="controls:AdvobotTitle">
            <Setter Property="VerticalAlignment"
                    Value="Center" />
            <Setter Property="TextWrapping"
                    Value="WrapWithOverflow" />
        </Style>
        <Style TargetType="controls:AdvobotButton">
            <Setter Property="Template"
                    Value="{StaticResource ButtonControlTemplate}" />
        </Style>
        <Style TargetType="controls:AdvobotRepeatButton">
            <Setter Property="Template"
                    Value="{StaticResource ButtonControlTemplate}" />
        </Style>
    </Application.Resources>
</Application>
